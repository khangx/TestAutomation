package com.qa.listener;

/**
 * Created by Robbie on 10/6/2016.
 * Used for various logging types which affects HTML Report style logging. Used as parameter for QALogger static methods
 * STEP - Default
 */
public enum LogLevel {
    INFO, DEBUG, STEP, WARN, ERROR, FATAL
}
